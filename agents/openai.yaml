interface:
  display_name: "Mindharp"
  short_description: "A state-parser: the large-model mechanics, running on a human."
  default_prompt: "Use $mindharp to parse the user's state with engineer language and reach a steadier baseline."

policy:
  allow_implicit_invocation: false
